# Настройка Artifacts в TeamCity для просмотра отчетов

## Проблема: отчеты не видны в Artifacts

Если вкладки "Artifacts" нет или отчеты не видны, нужно настроить **Artifacts paths** в каждом шаге сборки.

## 📋 Где создаются отчеты

Скрипты создают отчеты в следующих местах:

1. **cppcheck:** `server/cppcheck-report/`
2. **Clang Static Analyzer:** `server/report-clang/`
3. **Trufflehog:** `trufflehog-report/` (в корне проекта)

## ⚙️ Как настроить Artifacts в TeamCity

### Шаг 1: cppcheck

1. Открой шаг **"Static Analysis: cppcheck"** (или "cppcheck")
2. Прокрути вниз до раздела **"Artifacts paths"**
3. Добавь путь:
   ```
   server/cppcheck-report/** => static-analysis/cppcheck/
   ```
4. Сохрани

### Шаг 2: Clang Static Analyzer

1. Открой шаг **"Static Analysis: Clang Static Analyzer"** (или "clang")
2. Прокрути вниз до раздела **"Artifacts paths"**
3. Добавь путь:
   ```
   server/report-clang/** => clang-analyzer-report/
   ```
4. Сохрани

### Шаг 3: Trufflehog

1. Открой шаг **"Static Analysis: Trufflehog"** (или "trufflehog")
2. Прокрути вниз до раздела **"Artifacts paths"**
3. Добавь путь:
   ```
   trufflehog-report/** => static-analysis/trufflehog/
   ```
4. Сохрани

## 🔍 Как проверить что отчеты создаются

### Вариант 1: Через логи сборки

1. Открой страницу сборки
2. Перейди на вкладку **"Build Log"**
3. Найди строки:
   - `✅ cppcheck analysis completed successfully`
   - `✅ Clang Static Analyzer analysis completed successfully`
   - `✅ Trufflehog scan completed`
4. Проверь что есть строки типа:
   - `📄 См. отчет: ./cppcheck-report/index.html`
   - `📄 См. отчет: ./report-clang/index.html`

### Вариант 2: Добавить вывод списка файлов в скрипты

Можно добавить в конец каждого скрипта:

```bash
echo "=== Список созданных файлов ==="
find ./server -name "*.html" -o -name "*.xml" -o -name "*.txt" | head -20
ls -la ./server/cppcheck-report/ 2>/dev/null || echo "cppcheck-report не найден"
ls -la ./server/report-clang/ 2>/dev/null || echo "report-clang не найден"
```

## 📂 Альтернативный способ: найти отчеты вручную

Если Artifacts не настроены, отчеты все равно создаются на агенте TeamCity:

1. Подключись к серверу с TeamCity агентом
2. Перейди в рабочую директорию агента:
   ```bash
   cd /opt/buildagent/work/ed5cb693892de379/server
   ```
3. Проверь наличие папок:
   ```bash
   ls -la cppcheck-report/
   ls -la report-clang/
   ```

## ✅ После настройки Artifacts

После добавления Artifacts paths:

1. Запусти сборку заново
2. После завершения открой страницу сборки
3. Должна появиться вкладка **"Artifacts"**
4. В ней будут папки:
   - `static-analysis/cppcheck/`
   - `clang-analyzer-report/`
   - `static-analysis/trufflehog/`

## 🚨 Если Artifacts все еще не видно

1. Проверь что шаги сборки **завершились успешно** (зеленый статус)
2. Проверь логи - есть ли сообщения об ошибках создания отчетов
3. Убедись что пути в Artifacts paths **точно совпадают** с путями в скриптах
4. Попробуй использовать **абсолютные пути** или **wildcards**:
   ```
   **/cppcheck-report/** => static-analysis/cppcheck/
   **/report-clang/** => clang-analyzer-report/
   ```
