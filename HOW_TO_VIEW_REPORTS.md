# Как посмотреть отчеты статического анализа в TeamCity

## 📊 Где находятся отчеты

После успешной сборки отчеты доступны в **Artifacts** (артефакты сборки).

### 1. Через веб-интерфейс TeamCity

1. Открой страницу **сборки** (build)
2. Перейди на вкладку **"Artifacts"**
3. Найди папки с отчетами:
   - 📁 `static-analysis/cppcheck/` → открой `index.html`
   - 📁 `clang-analyzer-report/` → открой `index.html`
   - 📁 `static-analysis/trufflehog/` → открой `report.txt` или `report.json`

### 2. Настройка Report Tabs (рекомендуется)

Для удобного просмотра отчетов прямо на странице сборки:

#### Настройка вкладки cppcheck:

1. **Project Settings** → **Report Tabs** → **Create new report tab**
2. Заполни:
   - **Tab title:** `cppcheck Report`
   - **Start page:** `static-analysis/cppcheck/index.html`
3. Сохрани

#### Настройка вкладки Clang Static Analyzer:

1. **Project Settings** → **Report Tabs** → **Create new report tab**
2. Заполни:
   - **Tab title:** `Clang Static Analyzer Report`
   - **Start page:** `clang-analyzer-report/index.html`
3. Сохрани

После настройки на странице каждой сборки появятся вкладки с отчетами!

### 3. Прямые ссылки на отчеты

После сборки можно открыть отчеты напрямую по URL:

```
https://your-teamcity-server/viewLog.html?buildId=BUILD_ID&tab=artifacts#static-analysis/cppcheck/index.html
https://your-teamcity-server/viewLog.html?buildId=BUILD_ID&tab=artifacts#clang-analyzer-report/index.html
```

Замени `BUILD_ID` на ID твоей сборки.

## 📋 Что показывают отчеты

### cppcheck Report
- **Качество кода:** утечки памяти, неиспользуемые переменные, логические ошибки
- **Формат:** HTML с цветовой индикацией проблем
- **Критичность:** BLOCKER проблемы → билд падает

### Clang Static Analyzer Report
- **Безопасность:** buffer overflow, use-after-free, null pointer, memory leaks
- **Формат:** HTML с детальным описанием проблем
- **Критичность:** предупреждения → билд продолжается

### Trufflehog Report
- **Секреты:** API ключи, пароли, токены в коде и Git истории
- **Формат:** текстовый или JSON
- **Критичность:** любой секрет → билд падает

## ✅ Проверка что все работает

1. Запусти сборку
2. После завершения открой вкладку **Artifacts**
3. Проверь наличие папок:
   - `static-analysis/cppcheck/`
   - `clang-analyzer-report/`
   - `static-analysis/trufflehog/`
4. Открой `index.html` в каждой папке

Если отчеты не появились - проверь настройки **Artifacts paths** в шагах сборки!
